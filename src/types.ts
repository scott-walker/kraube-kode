/**
 * Backward-compatible barrel re-export.
 * All types are defined in core/types/. This file exists so that
 * existing renderer imports (`from '../types'`) keep working.
 */
export * from './core/types';
